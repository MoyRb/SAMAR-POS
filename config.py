## config.py
# Configuración global del proyecto y punto de referencia para valores comunes.
# Actualmente el archivo se mantiene vacío porque la configuración se maneja
# directamente en los módulos especializados (utils.settings y utils.database),
# pero se preserva como placeholder documentado para futuras variables de entorno
# o constantes compartidas.
